# Atlas release notes

Empty until the first Atlas release lands (A6-4). Each release adds
`X.Y.Z.md` here, the file the `atlas-vX.Y.Z` job reads as the GitHub Release
body; the workflow refuses an `atlas-v` tag whose notes file does not exist.
