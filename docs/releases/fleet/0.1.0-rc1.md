# Mercury Fleet 0.1.0-rc1

> **This release was never published.** `@aywengo/mercury-fleet` does not exist on the npm registry
> (`404`), there is no `fleet-v0.1.0-rc1` tag, and there is no GitHub Release for it. The text below
> originally stated that this version was "published to the npm dist-tag `rc`" and gave
> `npm install -g @aywengo/mercury-fleet@rc` as a working command. Neither was ever true, and the
> install command cannot work. The release workflow says so directly: a fleet publish "has nothing to
> authenticate against".
>
> The file is kept because it documents the intended Fleet packaging and versioning scheme, which is
> still how a real Fleet release would work. Nothing in it is installable today.

Fleet is a separate package with its own version, so its RC would be tagged independently of the host
(`fleet-v0.1.0-rc1` vs `host-v0.1.0-rc1`) and the two need not move together. A real Fleet release
would go to the npm dist-tag **`rc`** rather than `latest`, so a plain
`npm install @aywengo/mercury-fleet` would not pick up a prerelease.

Fleet is a separate package with its own version, so its RC is tagged independently of the host
(`fleet-v0.1.0-rc1` vs `host-v0.1.0-rc1`) and the two need not move together.

First public release of Fleet. Phase 0: host registry and probe. No dispatch.

## Install

From the same checkout as the host:

```bash
npm run fleet -- hosts list
```

Or:

```bash
# not installable: @aywengo/mercury-fleet is not on the registry
npm run fleet -- --help
```

Requires Node.js ≥ 22.18. Operator guide: [`fleet/README.md`](../../../fleet/README.md).

`fleet --version` prints `mercury-fleet 0.1.0-rc1`.
`GET /healthz` includes `"product": "fleet"` and `"version": "0.1.0-rc1"`.

## What this is

Register independent Mercury hosts, probe them, and run `fleet serve`. Fleet
speaks HTTP only and does not import host code or share a database.

## Not in this release

Starting Runs, routing, or moving Runs between hosts.

Full notes: [`fleet/CHANGELOG.md`](../../../fleet/CHANGELOG.md).
