# Mercury logrotate config (for file-based logging deployments).
# Install: sudo cp logrotate.conf /etc/logrotate.d/mercury
/var/log/mercury/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
