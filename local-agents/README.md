# local-agents

Drop one JSON `LocalAgentConfig` file per agent here (default
`MERCURY_LOCAL_AGENTS_DIR`). Each file registers a declarative local CLI agent.

Schema + examples: [`../docs/agent-adapters.md`](../docs/agent-adapters.md) §4.
