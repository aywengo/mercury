# remote-agents

Drop one JSON `RemoteAgentConfig` file per agent here (default
`MERCURY_REMOTE_AGENTS_DIR`). Each file registers a declarative remote API agent
(e.g. Devin, OpenHands server).

Schema + examples: [`../docs/agent-adapters.md`](../docs/agent-adapters.md) §5.
